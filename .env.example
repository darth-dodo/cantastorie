# Cantastorie environment configuration
# Copy to .env and fill in values. The .env file is gitignored.

# Anthropic API key — used by the authoring pipeline for story generation.
# Not needed to run the player, which is fully static.
ANTHROPIC_API_KEY=
